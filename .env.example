# Supabase (required)
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Server-only (required for server actions that use admin privileges)
SUPABASE_SERVICE_ROLE_KEY=

# Direct Postgres connection (required if using app/actions/setup-db.ts)
DATABASE_URL=
